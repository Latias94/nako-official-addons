# OMAPAD-020 Provider Descriptor Ownership

Date: 2026-05-25
Status: DONE

## Changes

- Added provider-owned config/default facts to `ProviderCatalogEntry`.
- Moved provider enablement env vars and default-enabled decisions out of
  `ProviderId`.
- Moved provider-specific config loading into provider catalog entries.
- Moved provider proxy health fact logic from central `Config` matching into
  provider-owned descriptor functions.
- Kept public manifest defaults and existing provider order compatible.

## Validation

- `cargo fmt --all -- --check`
- `cargo nextest run -p nako-metadata-scraper provider registry config addon_manifest --no-fail-fast`
- `git diff --check`

## Handoff

OMAPAD-030 should make provider assembly single-pass so ready adapters,
diagnostics, and route health facts come from one source of truth.
