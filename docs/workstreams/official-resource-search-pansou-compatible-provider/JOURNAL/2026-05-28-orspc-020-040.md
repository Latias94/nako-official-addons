# ORSPC-020 Through ORSPC-040 Journal

Implemented the optional PanSou-compatible HTTP provider adapter.

Changes:

- Added disabled-by-default `PansouProviderConfig`.
- Added provider registration only when enablement and HTTP(S) base URL are
  both present.
- Added PanSou-compatible `/api/search` request shaping with `kw`, `conc`,
  `refresh`, `res`, `src`, `plugins`, `cloud_types`, and `ext`.
- Added response mapping for PanSou `results` and fallback `merged_by_type`.
- Updated manifest configuration schema, addon docs, compose example, Docker
  env defaults, and root README.

Validation:

- `cargo nextest run -p nako-resource-search --no-fail-fast` passed.
- `cargo fmt -p nako-resource-search -- --check` passed.
- `git diff --check -- Cargo.toml Cargo.lock README.md crates/nako-resource-search addons/resource-search docs/workstreams/official-resource-search-pansou-compatible-provider` passed with the existing Windows line-ending warning for `Cargo.lock`.
- `cargo build -p nako-resource-search` passed.
- `pwsh -File addons/resource-search/smoke.local.ps1 -SidecarBaseUrl http://127.0.0.1:9130` passed against a temporary local sidecar process.

Not run:

- Live PanSou-compatible service smoke.
