# OMPER-020 Journal

Date: 2026-05-24

## Summary

- Updated TMDB candidate enrichment to skip a failed movie candidate after HTTP runtime policy is
  exhausted.
- Updated Bangumi candidate enrichment to skip a failed subject candidate after HTTP runtime policy
  is exhausted.
- Search request failures still propagate as provider-level failures.
- Added provider `suggest` tests for partial candidate salvage.

## Verification

- `cargo nextest run -p nako-metadata-scraper tmdb bangumi --no-fail-fast`: PASS, 14 tests.

## Notes

- The providers currently return an empty candidate set if every candidate enrichment fails. A
  user-facing partial-warning model would need a separate payload and UX decision.
