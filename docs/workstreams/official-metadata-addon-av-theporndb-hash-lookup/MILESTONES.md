# Milestones

Status: Complete
Last updated: 2026-05-26

## M1 - Workstream Open

Exit criteria:

- Workstream docs exist.
- `WORKSTREAM.json` is valid JSON.
- Hash alias scope is explicit.

Result: Done.

## M2 - Hash Lookup Slice

Exit criteria:

- `file_oshash` and `file_phash` are declared provider external-id capabilities.
- Top-level aliases parse through the existing metadata query path.
- ThePornDB calls `/scenes/hash/{hash}` with `type=OSHASH` or `type=PHASH`.
- Returned hash data maps into candidate external IDs.

Result: Done.

## M3 - Closeout

Exit criteria:

- Targeted and package gates pass.
- README and addon docs mention hash aliases.
- Follow-ups distinguish scene hash, movie hash, and future query value object.

Result: Done.
