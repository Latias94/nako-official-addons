# OMBGM-030 - Bangumi API Adapter

Status: DONE
Date: 2026-05-23

## Changes

- Replaced the no-result placeholder with a real `BangumiMetadataProvider`.
- Implemented `POST /v0/search/subjects` with bounded `limit` and `offset`.
- Implemented `GET /v0/subjects/{subject_id}` detail enrichment.
- Sent the configured User-Agent through `ProviderHttpRuntimeConfig`.
- Sent optional bearer auth when `NAKO_METADATA_SCRAPER_BANGUMI_ACCESS_TOKEN`
  is configured.
- Mapped subject data into metadata patch fields and provider-neutral facts.

## Validation

- `cargo nextest run -p nako-metadata-scraper bangumi --no-fail-fast` passed
  with 3 tests and 31 skipped.
- `cargo fmt --all -- --check` passed.
- `git diff --check` passed with only the Cargo.lock line-ending warning.

## Notes

- Default tests use synthetic responses only.
- No live Bangumi network gate was added.
- Douban and browser/crawler runtime remain out of scope.
