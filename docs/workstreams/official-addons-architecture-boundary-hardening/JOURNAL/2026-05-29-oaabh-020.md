# OAABH-020 Journal

Date: 2026-05-29
Task: OAABH-020 - Official Manifest Source Of Truth

## Changes

- Added `nako-official-addon-catalog` as a workspace dependency for
  `nako-resource-search`, `nako-subtitle-provider`, and `nako-dlna-renderer`.
- Replaced duplicated runtime manifest construction in subtitle-provider and
  DLNA renderer with catalog `manifest_with_version` builders.
- Reworked resource-search manifest construction to start from the catalog
  builder, while keeping runtime provider configuration fragments sidecar-owned.
- Kept checked-in example manifest compatibility through existing manifest
  tests.

## Evidence

- `cargo fmt -p nako-resource-search -p nako-subtitle-provider -p nako-dlna-renderer -- --check`
  passed.
- `cargo nextest run -p nako-resource-search -p nako-subtitle-provider -p nako-dlna-renderer manifest --no-fail-fast`
  passed with 10 tests.
- `cargo nextest run -p nako-official-addon-catalog resource_search subtitle dlna --no-fail-fast`
  passed with 6 tests in `../nako`.
- `cargo nextest run -p nako-resource-search -p nako-subtitle-provider -p nako-dlna-renderer --no-fail-fast`
  passed with 78 tests.
- `git diff --check` passed; Git emitted a Windows line-ending warning for
  `Cargo.lock`.

## Notes

- Resource-search still owns its expanded runtime configuration schema because
  provider fragments expose defaults from the active sidecar configuration.
- `../nako` has unrelated dirty `web` files and screenshot artifacts and must
  not be cleaned or edited by this lane.
