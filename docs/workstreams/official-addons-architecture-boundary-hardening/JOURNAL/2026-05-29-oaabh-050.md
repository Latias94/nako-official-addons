# OAABH-050 Journal

Date: 2026-05-29
Task: OAABH-050 - Notification Bridge Route Locality

## Changes

- Moved provider send fan-out into `provider_send`.
- Moved diagnostics HTML rendering into `diagnostics`.
- Kept route handlers responsible for HTTP extraction, envelope checks,
  template rendering, and response payload assembly.
- Preserved provider attempt-history recording and safe provider error mapping.
- Added focused module tests for provider send outcomes and diagnostics
  redaction.

## Evidence

- `cargo fmt -p nako-notification-bridge` passed.
- `cargo check -p nako-notification-bridge --tests` passed.
- `cargo nextest run -p nako-notification-bridge routes diagnostics provider --no-fail-fast`
  passed with 36 tests.
- `cargo nextest run -p nako-notification-bridge --no-fail-fast` passed with
  46 tests.
- `cargo fmt -p nako-notification-bridge -- --check` passed.

## Notes

- Public route response payloads stayed route-owned and covered by existing
  integration-style route tests.
- `provider_send` does not own route validation. It owns provider client fan-out
  and attempt-history side effects.
