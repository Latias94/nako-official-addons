# OAABH-030 Journal

Date: 2026-05-29
Task: OAABH-030 - Nako Addon App Service Boundary Deepening

## Changes

- Split the oversized Nako addon app service into local modules:
  `catalog`, `surfaces`, `routing`, `diagnostics`, `resource_search`, and
  `subtitles`.
- Kept the parent `addons.rs` focused on service construction, registration,
  lifecycle status, token/grant management, and shared helpers.
- Preserved public Admin API behavior and existing test names; this was a
  module-boundary refactor only.

## Evidence

- `cargo check -p nako-server --tests` passed in `../nako`.
- `cargo nextest run -p nako-server addon --no-fail-fast` passed with 117 tests.

## Notes

- `../nako` has unrelated dirty web files. They were not edited as part of this
  task.
- Parent `addons.rs` is now 926 lines; extracted modules contain the
  user-facing addon workflows with tighter local imports.
