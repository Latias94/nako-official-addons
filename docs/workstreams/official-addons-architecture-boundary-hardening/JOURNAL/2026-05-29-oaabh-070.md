# OAABH-070 Journal

Date: 2026-05-29
Task: OAABH-070 - Closeout

## Changes

- Closed the architecture boundary hardening lane after OAABH-020 through
  OAABH-060 completed.
- Recorded final focused gates for touched official-addon packages.
- Recorded server-only validation for the cross-repo `../nako` addon service
  boundary split.
- Preserved unrelated dirty `../nako/web` and web workstream files.

## Evidence

- `python -m json.tool docs/workstreams/official-addons-architecture-boundary-hardening/WORKSTREAM.json`
  passed.
- `python -m json.tool docs/workstreams/official-metadata-addon-mature-provider-model-research/WORKSTREAM.json`
  passed.
- `cargo fmt -p nako-resource-search -p nako-subtitle-provider -p nako-dlna-renderer -p nako-metadata-scraper -p nako-notification-bridge -- --check`
  passed.
- `cargo nextest run -p nako-resource-search -p nako-subtitle-provider -p nako-dlna-renderer -p nako-metadata-scraper -p nako-notification-bridge --no-fail-fast`
  passed with 400 tests.
- `git diff --check` passed in `nako-official-addons`; Git emitted a Windows
  line-ending warning for `Cargo.lock`.
- `cargo fmt -p nako-server -- --check` passed in `../nako`.
- `cargo check -p nako-server --tests` passed in `../nako`.
- `cargo nextest run -p nako-server addon --no-fail-fast` passed with 117 tests
  in `../nako`.
- Path-scoped `git diff --check` passed for `../nako` server addon files; Git
  emitted a Windows line-ending warning for `addons.rs`.

## Residual Follow-Ups

- External Acquisition Runner remains intentionally split out.
- Actual provider cache/throttle execution state should only be added if
  provider limits make it necessary; the policy boundary now exists.
- Host-owned metadata policy context, local metadata, locked fields, provider
  order, and final merge/apply policy remain Nako core work, not sidecar work.
