# OMAFR-080 — Docs, Examples, And Deletion Sweep

Date: 2026-05-23
Status: DONE

## Work Completed

- Updated root README to describe the current runtime truth:
  fixture provider by default, TMDB movie-search proof disabled by default,
  future provider breadth behind the same seams.
- Updated addon README with local smoke commands and runtime defaults.
- Updated Dockerfile environment defaults to match `Config::default`.
- Replaced the misleading systemd TMDB token sample with an
  operator-managed `EnvironmentFile` pattern.
- Added `checked_in_example_manifest_matches_runtime_manifest` so
  `addons/metadata-scraper/manifest.example.json` cannot drift from runtime
  generation.

## Evidence

```text
cargo nextest run -p nako-metadata-scraper manifest --no-fail-fast
```

Result: pass. 5 tests run, 5 passed, 22 skipped by filter.

## Handoff

Continue with OMAFR-090 closeout. Remaining product breadth should be split
into follow-ons rather than stuffed into this architecture lane.
