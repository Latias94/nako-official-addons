# OMAFR-040 Journal

Date: 2026-05-23
Status: DONE

## Summary

OMAFR-040 introduced `MetadataScrapeRuntime` and moved metadata request
normalization, provider fan-out, candidate ordering, response shaping, and
provider failure swallowing out of `routes.rs`.

Key changes:

- Added `engine::MetadataScrapeRuntime`.
- Centralized request normalization inside the runtime.
- Centralized provider fan-out and candidate sorting inside the runtime.
- Kept route handlers as HTTP envelope adapters only.
- Updated fixture provider behavior to consume normalized query input.
- Added runtime tests for normalization, ordering, and failure swallowing.

## Validation

```text
cargo fmt --all -- --check
```

Result: pass.

```text
cargo nextest run -p nako-metadata-scraper metadata --no-fail-fast
```

Result: pass. 4 tests run, 4 passed, 13 skipped by filter.

```text
cargo nextest run -p nako-metadata-scraper --no-fail-fast
```

Result: pass. 17 tests run, 17 passed.

```text
cargo nextest run --workspace --no-fail-fast
```

Result: pass. 17 tests run, 17 passed.

```text
git diff --check
```

Result: pass.

## Next

Continue with OMAFR-050. Add a sidecar-owned provider HTTP runtime for real
provider adapters with timeout, retry, proxy, user-agent, response-size, and
redaction-safe error classification.
