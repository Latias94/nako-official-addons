# OMAFR-060 Journal

Date: 2026-05-23
Status: DONE

## Summary

OMAFR-060 added the first bounded real provider proof: TMDB movie search.

Key changes:

- Added `providers::tmdb::TmdbMetadataProvider`.
- Added `ProviderId::Tmdb` and TMDB environment configuration.
- Kept TMDB disabled by default.
- Made enabled TMDB unavailable when no read-access token is configured.
- Added manifest secret-reference field declaration when TMDB is enabled.
- Added synthetic TMDB mapping tests through fake HTTP transport.
- Updated addon README, compose, systemd, and example manifest for optional
  TMDB configuration without embedding real secrets.

## Validation

```text
cargo nextest run -p nako-metadata-scraper tmdb --no-fail-fast
```

Result: pass. 3 tests run, 3 passed, 23 skipped by filter.

```text
cargo fmt --all -- --check
```

Result: pass.

```text
cargo nextest run -p nako-metadata-scraper --no-fail-fast
```

Result: pass. 26 tests run, 26 passed.

```text
cargo nextest run --workspace --no-fail-fast
```

Result: pass. 26 tests run, 26 passed.

```text
git diff --check
```

Result: pass with a Cargo.lock line-ending warning.

## Next

Continue with OMAFR-070. Document and, where practical, script a local smoke
flow against `../nako`: start sidecar, register disabled, run health check,
enable, call metadata resource, and inspect redaction-safe diagnostics.
