# APQ-040 Journal

Date: 2026-05-26
Status: DONE

## Scope

Closed the AV parser-quality lane after migrating current AV provider families
to shared row-level label parsing and documenting the behavior.

## Evidence

- PASS: `cargo nextest run -p nako-metadata-scraper --no-fail-fast` passed with
  222 tests.
- PASS: `cargo fmt -p nako-metadata-scraper -- --check`.
- PASS: `python -m json.tool docs/workstreams/official-metadata-addon-av-parser-quality/WORKSTREAM.json`.
- PASS: `git diff --check`.

## Closeout Notes

- `rendered_av::structured_or_labeled_value` is now the shared AV detail label
  primitive.
- Providers keep their row selectors local; this avoids flattening different
  page models into one brittle shared parser.
- Future provider work should reuse this helper from the start.
