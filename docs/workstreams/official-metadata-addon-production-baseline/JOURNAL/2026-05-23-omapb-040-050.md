# OMAPB-040/050 — TMDB Baseline And Docs

Date: 2026-05-23
Status: DONE

## Work Completed

- Expanded TMDB provider from search-only proof to bounded movie baseline.
- TMDB now calls:
  - `/search/movie`;
  - `/movie/{id}`;
  - `/movie/{id}/external_ids`.
- Detail enrichment maps runtime minutes, tagline, overview, release date,
  detail genres, external IDs, and safe poster/backdrop path tags.
- README and addon README now describe TMDB baseline and provider-neutral
  ranking/evidence.

## Evidence

```text
cargo nextest run -p nako-metadata-scraper tmdb --no-fail-fast
```

Result: pass. 3 tests run, 3 passed.

```text
cargo fmt --all -- --check
```

Result: pass.

```text
cargo nextest run -p nako-metadata-scraper --no-fail-fast
```

Result: pass. 31 tests run, 31 passed.

## Handoff

Continue with OMAPB-060 closeout. Live Nako Admin-mediated smoke remains an
external evidence gap until a local Nako server and admin token are available.
