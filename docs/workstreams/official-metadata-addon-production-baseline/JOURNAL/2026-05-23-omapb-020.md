# OMAPB-020 — Live Nako Admin-Mediated Smoke

Date: 2026-05-23
Status: DONE_WITH_CONCERNS

## Work Completed

- Verified the new workstream JSON.
- Checked local Nako Admin API availability.
- Checked `NAKO_ADMIN_TOKEN` availability.
- Rebuilt the sidecar binary.
- Ran direct sidecar smoke through `smoke.local.ps1` against a temporary
  sidecar on `127.0.0.1:19101`.

## Evidence

```text
python -m json.tool docs\workstreams\official-metadata-addon-production-baseline\WORKSTREAM.json | Out-Null
```

Result: pass.

```text
Invoke-WebRequest http://127.0.0.1:3000/admin/v1/addons
```

Result: unavailable. TCP connection refused on `127.0.0.1:3000`.

```text
NAKO_ADMIN_TOKEN
```

Result: unset.

```text
cargo build -p nako-metadata-scraper
```

Result: pass.

```text
pwsh -NoProfile -File addons\metadata-scraper\smoke.local.ps1 -SidecarBaseUrl http://127.0.0.1:19101
```

Result: pass.

Observed safe output:

```text
[sidecar] Manifest OK: nako.official.metadata-scraper@0.1.0
[sidecar] Health OK; enabled providers: fixture
[sidecar] Metadata resource OK; candidates=1, artifacts=1
```

## Concern

Live Nako Admin-mediated smoke was not run because the local Nako server was
not listening on `127.0.0.1:3000` and no admin token was available in the
environment.

## Handoff

Continue with OMAPB-030 ranking/evidence. When a local Nako server and admin
token are available, rerun:

```powershell
$env:NAKO_ADMIN_TOKEN = '<admin bearer token>'
pwsh -File addons/metadata-scraper/smoke.local.ps1 `
  -SidecarBaseUrl http://127.0.0.1:9100 `
  -NakoBaseUrl http://127.0.0.1:3000 `
  -RegisterInNako `
  -Enable `
  -RunResourceCall `
  -RequireNako
```
