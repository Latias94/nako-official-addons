# OMAPB-030 — Provider-Neutral Ranking And Evidence

Date: 2026-05-23
Status: DONE

## Work Completed

- Added `engine::ranking`.
- Added provider-neutral `ProviderMetadataCandidate`,
  `ProviderCandidateFacts`, and `ProviderExternalId`.
- Moved final `confidence_milli` ownership into `MetadataScrapeRuntime`.
- Added deterministic candidate sorting by confidence, provider, and
  provider ID.
- Added redaction-safe evidence fields for title, year, language, external ID,
  and score reasons.
- Updated fixture and TMDB providers to return normalized facts rather than
  final provider-local scores.

## Evidence

```text
cargo nextest run -p nako-metadata-scraper ranking evidence --no-fail-fast
```

Result: pass. 5 tests run, 5 passed.

```text
cargo fmt --all -- --check
```

Result: pass.

```text
cargo nextest run -p nako-metadata-scraper --no-fail-fast
```

Result: pass. 31 tests run, 31 passed.

## Handoff

Continue with OMAPB-040. TMDB should now emit richer facts and artifacts while
letting runtime ranking compute final confidence.
