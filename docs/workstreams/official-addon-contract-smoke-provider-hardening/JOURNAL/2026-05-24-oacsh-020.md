# OACSH-020 Task Envelope Contract Unification

Date: 2026-05-24

## Changes

- Removed local `AddonTaskRequest` and `AddonTaskResponse` mirrors from
  `crates/nako-metadata-scraper/src/engine/bulk.rs`.
- Updated bulk planner and route handler code to use public
  `nako-addon-protocol` task envelope types.
- Kept `BulkMetadataScrapeTaskInput` strict and kept
  `BulkMetadataScrapeTaskOutput` sidecar-owned.

## Evidence

- `cargo nextest run -p nako-metadata-scraper bulk task routes manifest --no-fail-fast`:
  13 passed, 127 skipped.
- `cargo fmt --all -- --check`: passed.
- `git diff --check -- crates/nako-metadata-scraper/src/engine/bulk.rs crates/nako-metadata-scraper/src/routes.rs docs/workstreams/official-addon-contract-smoke-provider-hardening`:
  passed.

## Notes

The public protocol task envelope intentionally remains version-tolerant. This
lane did not move bulk result payload schemas or sidecar batch planning into
`nako-addon-protocol`.
