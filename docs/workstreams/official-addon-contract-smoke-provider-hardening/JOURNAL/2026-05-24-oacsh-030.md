# OACSH-030 Live Smoke Harness

Date: 2026-05-24

## Changes

- Hardened `addons/metadata-scraper/smoke.local.ps1` so Nako-owned flags
  require `-RegisterInNako` and cannot silently fall back to sidecar-only
  smoke.
- Added manifest task declaration/path assertion to the local smoke.
- Added writeback status and safe-error-code expectation flags.
- Added `-PreflightOnly` to `../nako/scripts/official-addon-e2e-smoke.ps1`
  to check script path, required commands, config, ports, and binary-mode
  assumptions without starting Docker.
- Updated README docs for the new smoke gate behavior.

## Evidence

- PowerShell parser checks passed for both smoke scripts.
- `../nako/scripts/official-addon-e2e-smoke.ps1 -AddonRepo F:\SourceCodes\Rust\nako-official-addons -AddonBinarySource workspace -PreflightOnly`:
  passed.
- `addons/metadata-scraper/smoke.local.ps1 -RunTaskPath` without
  `-RegisterInNako`: failed early with the expected diagnostic.
- `cargo nextest run -p nako-metadata-scraper bulk task routes manifest --no-fail-fast`:
  13 passed, 127 skipped.
- `cargo fmt --all -- --check`: passed.
- Path-scoped `git diff --check` passed in both repositories.

## Notes

Full Docker/server live smoke was not executed in this session. The script now
has a stronger preflight and cannot silently skip the Nako-owned path when a
caller requests it.
