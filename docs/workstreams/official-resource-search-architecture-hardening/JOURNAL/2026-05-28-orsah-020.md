# ORSAH-020 Journal

## Summary

- Split the resource search domain into dedicated query, link, and result
  modules.
- Added `ResourceSearchIntent` as an internal normalized intent model.
- Moved link construction into `links` so DTOs no longer depend on URL
  classification or normalization helpers.
- Updated fixture and PanSou-compatible adapters to use the new boundaries.

## Validation

- `cargo fmt -p nako-resource-search`: passed.
- `cargo fmt -p nako-resource-search -- --check`: passed.
- `cargo nextest run -p nako-resource-search --no-fail-fast`: passed, 31 tests.
- `git diff --check -- Cargo.toml Cargo.lock README.md crates/nako-resource-search addons/resource-search docs/workstreams/official-resource-search-architecture-hardening`:
  passed.

## Follow-up

- ORSAH-030 should introduce provider descriptors, source policy, and registry
  ownership before adding additional sources.
