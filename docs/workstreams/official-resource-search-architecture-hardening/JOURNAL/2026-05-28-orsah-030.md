# ORSAH-030 Journal

## Summary

- Added source policy names for official, external-service, and third-party
  provider ownership.
- Added provider descriptors and capability names.
- Moved provider activation into `ProviderRegistry`.
- Kept fixture as the only active default provider and left PanSou-compatible
  disabled until both enablement and a base URL are configured.
- Exposed registry diagnostics through the health payload.

## Validation

- `cargo fmt -p nako-resource-search`: passed.
- `cargo fmt -p nako-resource-search -- --check`: passed.
- `cargo nextest run -p nako-resource-search --no-fail-fast`: passed, 35 tests.
- `git diff --check -- Cargo.toml Cargo.lock README.md crates/nako-resource-search addons/resource-search docs/workstreams/official-resource-search-architecture-hardening`:
  passed.

## Follow-up

- ORSAH-040 should move fusion/ranking/dedupe out of `engine.rs` and introduce
  a provider batch result shape without expanding provider capabilities.
