# OMAPED-020 Journal

Date: 2026-05-25

## Summary

- Replaced the provider config optional-field matrix with typed
  `ProviderConfigKind` variants.
- Moved TMDB, Bangumi, browser_worker, and Douban config structs and provider
  env parsing into their provider modules.
- Kept central re-exports for compatibility with existing call sites.
- Renamed the stale browser_worker registry test to match the current default
  base URL behaviour.

## Validation

- `cargo nextest run -p nako-metadata-scraper config manifest provider registry --no-fail-fast`
  - Result: 94 tests run, 94 passed, 51 skipped.
- `cargo fmt --all -- --check`
- `python -m json.tool docs/workstreams/official-metadata-addon-provider-extension-decentralization/WORKSTREAM.json`
- `git diff --check`

## Status

DONE.
