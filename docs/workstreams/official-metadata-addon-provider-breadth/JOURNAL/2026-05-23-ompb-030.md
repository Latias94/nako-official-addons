# OMPB-030 Journal

Date: 2026-05-23

## Summary

- Added provider-neutral `alternate_titles` candidate facts.
- Updated ranking to match query titles against patch titles and provider alternate titles.
- Added TMDB alternative title enrichment through `movie/{id}/alternative_titles`.
- Added Bangumi alternate title extraction from original/localized names and title-like infobox
  entries.
- Added shared title normalization and raw-search-empty fallback search for TMDB and Bangumi.

## Verification

- `cargo nextest run -p nako-metadata-scraper tmdb bangumi ranking title --no-fail-fast`: PASS, 21
  tests.
- `cargo nextest run -p nako-metadata-scraper --no-fail-fast`: PASS, 64 tests.
- `cargo fmt --all -- --check`: PASS.
- `git diff --check`: PASS.

## Notes

- Search fallback intentionally runs only after an empty raw-title result set. Non-empty multi-search
  merging remains a follow-on because it changes provider request volume and deduplication policy.
