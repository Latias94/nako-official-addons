# OMPB-020 Artwork Selection Proof

Date: 2026-05-23

## Summary

Implemented provider-local artwork selection so the addon now chooses the best
poster or backdrop across all candidates instead of the first matching one.

## Validation

- `cargo nextest run -p nako-metadata-scraper artwork --no-fail-fast`
- `cargo nextest run -p nako-metadata-scraper --no-fail-fast`
- `cargo fmt --all -- --check`
- `git diff --check`

## Notes

- Selection now prefers higher-confidence candidates and uses resolution as a
  deterministic tiebreaker.
- The next slice is alias and localized title breadth.
