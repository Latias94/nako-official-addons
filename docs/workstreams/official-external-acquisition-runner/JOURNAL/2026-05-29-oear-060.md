# OEAR-060 Journal

Date: 2026-05-29
Task: OEAR-060 - Closeout
Status: Complete

## Final Status

Closed the Official External Acquisition Runner lane.

Completed scope:

- OEAR-010 opened the lane and froze boundaries.
- OEAR-020 added protocol, API, server, and official catalog action contracts.
- OEAR-030 added the fixture/no-op official runner sidecar and addon package.
- OEAR-040 guarded Nako host dispatch for typed external acquisition actions.
- OEAR-050 selected Transmission as the first production adapter candidate and
  split adapter prerequisites.

## Fresh Closeout Gates

- `cargo fmt -p nako-external-acquisition-runner -- --check`
- `cargo nextest run -p nako-external-acquisition-runner --no-fail-fast`
- `cargo check -p nako-server --tests`
- `cargo nextest run -p nako-server addon_external_acquisition_action --no-fail-fast`
- `python -m json.tool docs/workstreams/official-external-acquisition-runner/WORKSTREAM.json`
- `git diff --check -- docs/workstreams/official-external-acquisition-runner`

## Follow-Ons

1. Host-to-runner materialization contract:
   define how the sidecar can resolve an approved `selected_link_ref` or
   `intake_candidate_ref` into downloader material without browser raw URL
   submission, including authorization, expiry, audit, and redaction.
2. Transmission adapter lane:
   implement a Transmission runner profile after materialization exists, using
   `transmission:<hash_string>` as the stable runner job reference.
3. Existing clippy debt:
   `cargo clippy -p nako-server --tests -- -D warnings` remains blocked by
   unrelated package-wide lint debt and should be handled separately.

## Residual Risk

The fixture and host dispatch contract are complete, but no production adapter
should be built until materialization is defined. Without that follow-on, a real
adapter would be forced either to accept raw URLs or to invent an unsafe
side-channel, both of which violate this lane's core boundary.
