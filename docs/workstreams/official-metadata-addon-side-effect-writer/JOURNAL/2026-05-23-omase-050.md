# OMASE-050 - Bulk Metadata Scrape Task Evaluation

Task: evaluate whether the current Nako host exposes enough Addon Task seam
for Bulk Metadata Scrape, and declare or defer manifest task changes.

What changed:
- Confirmed Nako core has `AddonTaskDeclaration`, Admin registration
  validation, and routing plans targeting `AddonTaskJob`.
- Confirmed the generic Addon Task scheduler/invoker remains deferred in Nako
  docs and current host code.
- Kept the official metadata addon manifest task-free and added a manifest
  test that proves `tasks` remains empty until host runtime execution exists.
- Opened `official-metadata-addon-bulk-task-design` as the follow-on lane.

Validation:
- `cargo nextest run -p nako-metadata-scraper manifest bulk --no-fail-fast`
- `python -m json.tool docs/workstreams/official-metadata-addon-side-effect-writer/WORKSTREAM.json`
- `python -m json.tool docs/workstreams/official-metadata-addon-bulk-task-design/WORKSTREAM.json`
- `cargo nextest run --workspace --no-fail-fast`
- `git diff --check`

Notes:
- No hidden Addon-side background scheduler was added.
- The new bulk task line starts at OMAB-020: host task runtime contract.
