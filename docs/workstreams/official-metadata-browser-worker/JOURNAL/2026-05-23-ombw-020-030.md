# OMBW-020/030 Browser Worker Proof And Integration

Date: 2026-05-23
Status: DONE

## Summary

- Added `addons/browser-worker` as an internal Node service with Express, Crawlee, and Playwright.
- Added `GET /health`, `GET /fixtures/rendered-page`, and `POST /extract`.
- Proved deterministic rendered-page extraction with a local HTML fixture whose text is changed by JavaScript before extraction.
- Added the Rust `browser_worker` provider and wired it into config, registry diagnostics, manifest configuration defaults, README notes, and Docker Compose.
- Preserved the public addon boundary: `nako-metadata-scraper` calls the worker over HTTP and does not embed Playwright or Crawlee.

## Verification

- `npm test` in `addons/browser-worker`: passed; 1 test captured rendered DOM text from a local page.
- `npm run smoke` in `addons/browser-worker`: passed; health endpoint and `/extract` fixture path returned rendered JavaScript text.
- `cargo nextest run -p nako-metadata-scraper --no-fail-fast`: passed; 52 tests passed.
- `cargo nextest run --workspace --no-fail-fast`: passed; 52 tests passed.
- `docker compose -f addons/metadata-scraper/compose.example.yml config`: passed; compose topology includes `nako-browser-worker` and sidecar env wiring.
- `cargo fmt --all -- --check`: passed.
- `git diff --check`: passed.
- `rg -n "playwright|crawlee" Cargo.toml Cargo.lock crates addons/browser-worker addons/metadata-scraper -S`: matches are limited to `addons/browser-worker` implementation/lockfile and README wording, not Rust crate dependencies.

## Notes

- A temporary ad hoc dual-process integration smoke was attempted but the local PowerShell process orchestration failed before reaching a meaningful sidecar assertion. It is not recorded as product evidence.
- OMBW-040 remains open for the first real Douban-backed consumer or an explicit split if live access constraints require recorded fixtures.
