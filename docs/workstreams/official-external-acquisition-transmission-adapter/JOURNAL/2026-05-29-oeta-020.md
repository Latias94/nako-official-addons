# OETA-020 Journal - Transmission Profile And Secret Policy

Date: 2026-05-29

Added the opt-in Transmission profile configuration boundary.

Changes:

- Added `TransmissionConfig` with environment parsing for profile id, RPC URL,
  optional username/password, timeout, and invalid-TLS override.
- Redacted Transmission password, username, and RPC URL from Debug output.
- Added diagnostics that report profile readiness without exposing endpoint,
  credentials, or RPC payloads.
- Updated the checked-in external acquisition runner manifest example with the
  `transmission` profile schema.
- Updated the official Nako addon catalog in `../nako` with matching
  configuration schema and the optional `transmission_password` secret field.
- Documented Transmission environment variables while keeping fixture mode as
  the local default.

Validation:

- `cargo nextest run -p nako-external-acquisition-runner config manifest diagnostics --no-fail-fast`
- `cargo nextest run -p nako-official-addon-catalog external_acquisition_runner_default_manifest_matches_official_catalog_facts --no-fail-fast`
- `cargo fmt -p nako-external-acquisition-runner -- --check`
- `cargo fmt -p nako-official-addon-catalog -- --check`

Next task: OETA-030, Transmission RPC client harness.
