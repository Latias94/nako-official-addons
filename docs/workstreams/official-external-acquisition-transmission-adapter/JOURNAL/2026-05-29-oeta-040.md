# OETA-040 Journal - Transmission Enqueue Through Materialization

Date: 2026-05-29

Wired the Transmission profile enqueue path through host materialization.

Changes:

- Added injectable `TransmissionRunnerClient` so runner tests do not need a
  live Transmission daemon.
- Added Transmission profile routing while keeping fixture mode as the default.
- Enqueue for the `transmission` profile now materializes the approved action
  target and passes supported material to Transmission add.
- Returned `runner_job_ref` as `transmission:<hash_string>`.
- Mapped Transmission duplicate add to `AlreadyExists`.
- Rejected unsupported link types and password-bearing material with safe error
  codes.
- Preserved response redaction for raw URI, materialization ref, target ref,
  idempotency key, and job id.

Validation:

- `cargo nextest run -p nako-external-acquisition-runner transmission enqueue materialization --no-fail-fast`
- `cargo fmt -p nako-external-acquisition-runner -- --check`
- `cargo clippy -p nako-external-acquisition-runner --tests -- -D warnings`

Next task: OETA-050, status and control operations from
`transmission:<hash_string>` runner job refs.
