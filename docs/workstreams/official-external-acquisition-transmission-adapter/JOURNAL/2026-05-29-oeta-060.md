# OETA-060 Journal - Route And Smoke Integration

Date: 2026-05-29

Completed route/smoke integration for the Transmission adapter lane.

Changes:

- Added route-level test coverage for a Transmission profile task request using
  fake materialization and fake Transmission RPC.
- Verified the route response returns `transmission:<hash_string>` and safe
  facts without exposing raw material, materialization refs, selected-link refs,
  or idempotency keys.
- Re-ran the full external acquisition runner package suite.
- Verified local smoke remains fixture-only and does not require a live
  Transmission daemon.

Validation:

- `cargo nextest run -p nako-external-acquisition-runner --no-fail-fast`
- `cargo fmt -p nako-external-acquisition-runner -- --check`
- `cargo clippy -p nako-external-acquisition-runner --tests -- -D warnings`
- `pwsh -File addons/external-acquisition-runner/smoke.local.ps1 -SidecarBaseUrl http://127.0.0.1:19160`

Next task: OETA-070, closeout.
