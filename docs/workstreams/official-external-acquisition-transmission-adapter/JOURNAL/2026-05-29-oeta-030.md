# OETA-030 Journal - Transmission RPC Client Harness

Date: 2026-05-29

Added the typed Transmission RPC client boundary before wiring runner actions.

Changes:

- Added `transmission.rs` with `TransmissionClient`, a transport trait, and
  reqwest-backed transport.
- Added redacted `TransmissionRpcConfig` and client Debug output.
- Implemented Transmission session-id retry for HTTP 409 with
  `X-Transmission-Session-Id`.
- Implemented typed methods for `torrent-add`, `torrent-get`,
  `torrent-start`, and `torrent-stop`.
- Mapped add outcomes to added vs duplicate hash results.
- Added fake transport tests for session-id retry, duplicate add, torrent get,
  start/stop calls, and redacted transport errors.

Validation:

- `cargo nextest run -p nako-external-acquisition-runner transmission --no-fail-fast`
- `cargo fmt -p nako-external-acquisition-runner -- --check`
- `cargo clippy -p nako-external-acquisition-runner --tests -- -D warnings`

Source note:

- Method names and session-id behavior follow the Transmission RPC reference at
  `https://github.com/transmission/transmission/blob/main/docs/rpc-spec.md`.

Next task: OETA-040, route Transmission enqueue through host materialization.
