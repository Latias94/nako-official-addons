# ORSFP-010 Journal

Opened `official-resource-search-first-class-protocol` because existing
resource-search lanes are complete and the protocol migration changes the
public manifest/resource contract.

Decision:

- Drop the temporary automation alpha contract from mainline.
- Use first-class Nako protocol DTOs at the route boundary.
- Keep provider internals on addon-owned domain types.
- Keep Admin UI and all external write/action contracts out of scope.
