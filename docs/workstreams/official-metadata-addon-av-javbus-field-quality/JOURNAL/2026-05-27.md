# Journal - 2026-05-27

## Resume

- Used `resume-workstream` to reconstruct state.
- Confirmed relevant AV workstreams are complete or closed.
- Opened this follow-up lane because JavBus field quality is a separate durable problem.

## Reference Notes

- `repo-ref/mdcx` is GPLv3/reference-only.
- Useful behavior: direct detail first, typed search fallback, JavBus as a strong source for release/runtime/actors/studio/publisher/director/series/images, and batch orchestration with concurrency and resumable failure accounting.
- Nako already owns orchestration through provider route filtering, merge policy, and `bulk-metadata-scrape`; this lane should strengthen provider facts.

## Implementation Notes

- Added direct-detail-first JavBus behavior with search fallback.
- Added richer JavBus detail fixture coverage for label rows, actor links, studio/publisher/director/series links, primary image hrefs, and sample images.
- Added browser-worker `headers` and bounded `actions` render options.
- Added optional `NAKO_METADATA_SCRAPER_JAVBUS_COOKIE` and manifest secret declaration.
- Added age-verification page rejection so access gates do not become false metadata candidates.

## Live Evidence

- Browser-worker with proxy reached JavBus.
- Without an operator JavBus cookie, the live page returned an age-verification flow and then a regional driving quiz.
- Live drift now reports `candidate_count: 0`, which is the correct result until `NAKO_METADATA_SCRAPER_JAVBUS_COOKIE` is configured.
