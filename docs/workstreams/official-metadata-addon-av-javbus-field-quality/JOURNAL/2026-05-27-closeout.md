# Journal - 2026-05-27 Closeout

## Completed

- JavBus now prefers direct detail pages before search fallback.
- Search pages that are already detail pages remain supported.
- JavBus parses richer synthetic detail facts: release date, runtime, actors, genres, studio, publisher, director, series, primary image, and sample images.
- Browser-worker render requests support bounded `headers` and `actions`.
- JavBus can send an optional operator cookie through `NAKO_METADATA_SCRAPER_JAVBUS_COOKIE`.
- JavBus age-verification pages are rejected as access gates rather than emitted as false candidates.

## Verification

- `cargo nextest run -p nako-metadata-scraper javbus rendered_av --no-fail-fast`: passed, 9 tests.
- `cargo nextest run -p nako-metadata-scraper javbus rendered_page manifest --no-fail-fast`: passed, 17 tests.
- `npm --prefix addons/browser-worker test`: passed, 4 tests.
- `cargo nextest run -p nako-metadata-scraper --no-fail-fast`: passed, 245 tests, 3 skipped.
- `cargo fmt -p nako-metadata-scraper -- --check`: passed.
- `python -m json.tool docs/workstreams/official-metadata-addon-av-javbus-field-quality/WORKSTREAM.json`: passed.
- `git diff --check`: passed with line-ending conversion warnings for edited browser-worker JS files.

## Residual Risk

Live JavBus field-rich scraping needs an operator cookie in this network location. Without it, the site returns an age/region verification flow and the provider correctly returns no candidates.
