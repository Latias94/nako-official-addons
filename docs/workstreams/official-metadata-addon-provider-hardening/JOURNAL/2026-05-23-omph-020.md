# OMPH-020 Network Policy Proof

Date: 2026-05-23
Status: Done

## Scope

- Added TMDB and Bangumi provider proxy URL configuration.
- Propagated provider proxy policy into the provider HTTP runtime for TMDB and
  Bangumi.
- Surfaced proxy policy through health diagnostics and the diagnostics hosted
  page as boolean state only.
- Documented the proxy environment variables and redaction behavior in addon
  docs.

## Validation

- `cargo nextest run -p nako-metadata-scraper provider_http_runtime config routes --no-fail-fast`: passed, 14 tests passed and 42 skipped.
- `cargo fmt --all -- --check`: passed.
- `git diff --check`: passed.

## Review

- No blocking OMPH-020 workstream-compliance findings.
- No blocking code-quality findings for the network-policy seam.
- Package-wide nextest is not used as the OMPH-020 completion gate because the
  current workspace also contains unrelated uncommitted `engine/ranking.rs`
  provider-quality changes that fail one ranking test.

## Next

- Continue with OMPH-030 for TMDB/Bangumi provider-quality and ranking work.
