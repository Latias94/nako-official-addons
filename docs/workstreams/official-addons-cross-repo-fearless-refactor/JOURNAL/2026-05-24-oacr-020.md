# OACR-020 Protected-Write Client Alignment

Date: 2026-05-24

## Changes

- Added protected-write runtime wire DTOs to `../nako/crates/nako-addon-protocol`.
- Added `NakoRuntimeClient` and runtime submission helpers to
  `../nako/crates/nako-addon-client`.
- Replaced metadata scraper private runtime DTO/client duplication with a thin
  facade over the public client/protocol crates.
- Updated directly affected Nako server `AddonClientError` match arms for the
  new safe error categories.

## Evidence

- `cargo nextest run -p nako-addon-client runtime --no-fail-fast`: 6 passed, 8
  skipped.
- `cargo nextest run -p nako-addon-protocol protected_write_payload_contracts_keep_wire_shape --no-fail-fast`:
  1 passed, 10 skipped.
- `cargo nextest run -p nako-metadata-scraper nako_runtime writeback artwork --no-fail-fast`:
  10 passed, 130 skipped.
- Path-scoped `git diff --check` passed in both repositories.

## Notes

- The sidecar still owns config loading and fake transport seams.
- Crate publishing/versioning is a release follow-on, not part of this lane.
