# OACR-050 Official Addon Task Path Smoke

Date: 2026-05-24

## Changes

- Added `-RunTaskPath` to `addons/metadata-scraper/smoke.local.ps1`.
- Added routing-plan creation, direct task-run creation, task polling, terminal
  status checks, and bounded `bulk-metadata-scrape` output assertions.
- Updated `../nako/scripts/official-addon-e2e-smoke.ps1` to forward the task
  smoke flag.
- Documented the new task path smoke in root and addon READMEs.

## Evidence

- PowerShell parser checks passed for both smoke scripts.
- `cargo nextest run -p nako-metadata-scraper bulk task routes manifest --no-fail-fast`:
  13 passed, 127 skipped.
- `cargo nextest run -p nako-server addon_task_run_direct_dispatch --no-fail-fast`:
  6 passed, 281 skipped.

## Notes

- Live Docker/server smoke was not executed because this session did not own a
  running Nako server/admin token.
