# 2026-05-26 Closeout

Implemented ThePornDB as a token/API AV provider rather than a rendered-page
provider. MDCx was used only as strategy inspiration: multi-source AV coverage,
field-priority merging, direct lookup aliases, proxy/operator knobs, and batch
drift checks. No GPL source, selectors, fixtures, regex tables, comments, or
structure were copied.

Delivered:

- `theporndb` provider with bearer-token auth, Rust-side proxy config, scene
  search, explicit scene detail lookup, and redaction-safe config behavior.
- Metadata mapping for title, overview, release date/year, duration, rating,
  performers, directors, site/network, tags, poster/backdrop/trailer media,
  external IDs, and source links.
- Registry/config/manifest/health/live-drift wiring, including secret reference
  fields and unavailable diagnostics when enabled without a token.
- README coverage for token, proxy, presets, direct lookup aliases, and field
  policy behavior.

Evidence:

- `cargo nextest run -p nako-metadata-scraper theporndb config registry manifest av_provider_preset field_policy av_drift health_endpoint --no-fail-fast`
- `cargo nextest run -p nako-metadata-scraper --no-fail-fast`
- `cargo fmt -p nako-metadata-scraper -- --check`
- `python -m json.tool docs/workstreams/official-metadata-addon-av-theporndb-provider/WORKSTREAM.json`
- `python -m json.tool addons/metadata-scraper/manifest.example.json`
- `git diff --check`

Follow-ups:

- Add explicit hash facts before using ThePornDB hash endpoints.
- Add movie-route support after scene/movie intent exists in the provider
  contract.
