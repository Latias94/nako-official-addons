# OMRQ-030 Journal - 2026-05-23

## Summary

Improved provider signal quality for TMDB and Bangumi without changing the
Addon Protocol contract.

## What Changed

- Added unified `community_score_milli` and `community_vote_count` facts.
- TMDB now maps vote average and vote count from search/detail responses.
- Bangumi now maps rating score and total vote count into the same facts.
- Runtime applies a small generic community-score bonus during ranking.
- Provider-local ordering logic was not added.

## Validation

- `cargo nextest run -p nako-metadata-scraper --no-fail-fast`
- `cargo nextest run --workspace --no-fail-fast`
- `cargo fmt --all -- --check`
- `git diff --check`

## Notes

- The protocol envelope and resource schema are unchanged.
- The quality signal is intentionally small and generic.
