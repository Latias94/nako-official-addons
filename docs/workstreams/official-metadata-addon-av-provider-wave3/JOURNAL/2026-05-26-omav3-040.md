# OMAV3-040 Journal

Date: 2026-05-26
Status: DONE

## Work Completed

- Added `prestige` as the first wave 3 AV provider.
- Used MDCx only as a reference for mature strategy: Prestige official search
  API plus product detail API. No GPL source, selectors, regex tables, comments,
  or fixtures were copied.
- Added disabled-by-default config, manifest schema/default support, provider
  catalog entry, external ID capabilities, aliases, provider outcome note, and
  field-quality descriptor.
- Implemented censored-route AV search, explicit `prestige_id` direct lookup,
  explicit `prestige_url` direct lookup, official detail mapping, artwork,
  trailer URL, and redaction-safe proxy policy diagnostics.
- Updated operator docs and addon README behavior notes.

## Validation

- `cargo nextest run -p nako-metadata-scraper prestige --no-fail-fast`: PASS, 4 tests.
- `cargo nextest run -p nako-metadata-scraper prestige config registry manifest av --no-fail-fast`: PASS, 62 tests.
- `cargo nextest run -p nako-metadata-scraper proxy routes --no-fail-fast`: PASS, 12 tests.
- `cargo fmt -p nako-metadata-scraper -- --check`: PASS.
- `python -m json.tool docs/workstreams/official-metadata-addon-av-provider-wave3/WORKSTREAM.json`: PASS.
- `git diff --check`: PASS.

## Notes

- Prestige uses direct Rust HTTP JSON runtime and
  `NAKO_METADATA_SCRAPER_PRESTIGE_PROXY_URL`; it does not need the browser
  worker.
- Default AV field policy now prefers Prestige for official, artwork, and
  trailer fields when Prestige participates in a merged AV cluster.
- Next task is OMAV3-050: evaluate an FC2 long-tail source and add one provider
  only if it improves fallback coverage beyond the existing FC2 official source.
