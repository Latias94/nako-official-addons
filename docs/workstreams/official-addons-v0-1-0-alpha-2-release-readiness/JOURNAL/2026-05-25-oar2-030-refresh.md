# OAR2-030 Release Readiness Refresh

Date: 2026-05-25
Status: BLOCKED

## Summary

Refreshed alpha.2 package-readiness evidence after the notification bridge
provider work.

No real crates.io publish was executed.

## Evidence

- `cargo metadata --format-version 1 --no-deps`: pass in
  `nako-official-addons`; both official addon crates are `0.1.0-alpha.2`.
- `cargo nextest run --workspace --no-fail-fast`: pass, 183 passed, 2 skipped.
- `cargo fmt --all -- --check`: pass.
- `git diff --check`: pass.
- `cargo publish -p nako-addon-protocol --locked --dry-run --allow-dirty` in
  `../nako`: pass, dry-run aborted before upload.
- `cargo publish -p nako-addon-client --locked --dry-run --allow-dirty` in
  `../nako`: blocked until `nako-addon-protocol 0.1.0-alpha.2` is published.
- `cargo publish -p nako-official-addon-catalog --locked --dry-run --allow-dirty`
  in `../nako`: blocked until `nako-addon-protocol 0.1.0-alpha.2` is
  published.
- `cargo publish -p nako-notification-bridge --locked --dry-run --allow-dirty`:
  blocked until `nako-addon-protocol 0.1.0-alpha.2` is published.
- `cargo publish -p nako-metadata-scraper --locked --dry-run --allow-dirty`:
  blocked until `nako-addon-client 0.1.0-alpha.2` is published; it will also
  require `nako-official-addon-catalog 0.1.0-alpha.2`.

## Publish Order

After explicit publish approval:

1. `nako-addon-protocol 0.1.0-alpha.2`
2. `nako-addon-client 0.1.0-alpha.2`
3. `nako-official-addon-catalog 0.1.0-alpha.2`
4. `nako-notification-bridge 0.1.0-alpha.2`
5. `nako-metadata-scraper 0.1.0-alpha.2`
6. `nako-chromecast-renderer 0.1.0-alpha.2`

Live E2E smoke remains separately blocked by Docker daemon availability.
