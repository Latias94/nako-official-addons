# Journal - 2026-05-26

## AVOPS-010

- Opened a new AV operations lane after AV parser-quality closeout.
- Split work into provider presets, manual live drift harness, and closeout.
- Preserved MDCx as reference-only and prohibited storing adult live payloads.

## AVOPS-020

- Added `NAKO_METADATA_SCRAPER_AV_PROVIDER_PRESET` with `manual`,
  `fast_safe`, `official_only`, `community_first`, `fc2_enhanced`, and
  `uncensored_official` strategies.
- Kept preset behavior in config so registry and providers remain preset
  agnostic.
- Explicit `NAKO_METADATA_SCRAPER_PROVIDER_*_ENABLED` values override preset
  defaults.
- Added manifest schema/default support and README documentation.
- Targeted preset/manifest nextest gate passed.

## AVOPS-030

- Added ignored manual AV drift smoke driven by
  `NAKO_METADATA_SCRAPER_LIVE_AV_PROVIDER_DRIFT_CASES`.
- The live harness uses `ProviderRegistry` and `MetadataProvider::suggest`, so
  browser-worker and proxy/session policy flow through the same runtime seam.
- Added deterministic tests for AV case parsing and redaction-safe
  field-health reports.

## AVOPS-040

- Full metadata-scraper package gate passed with 226 tests and 3 skipped
  ignored live tests.
- `cargo fmt -p nako-metadata-scraper -- --check`, workstream JSON validation,
  and `git diff --check` passed.
- Closed the lane. Recommended follow-up: Wave 4 provider breadth now that
  provider presets and live drift health checks are in place.
