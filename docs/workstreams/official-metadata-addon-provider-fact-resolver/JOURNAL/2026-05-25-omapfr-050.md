# OMAPFR-050 Journal

Date: 2026-05-25
Status: DONE

## Summary

- Ran the full `nako-metadata-scraper` package validation after OMAPFR-040.
- Updated root, crate, and addon README docs so user-facing docs no longer say
  runtime shaping only deduplicates exact duplicate candidates.
- Documented that top-level external ID aliases are capability-derived and that
  shared provider-emitted external IDs can resolve candidates before ranking.

## Evidence

- `cargo nextest run -p nako-metadata-scraper --no-fail-fast`
- `cargo fmt --all -- --check`
- `git diff --check`

## Notes

- No live provider smoke, release packaging, or reference-repository copy was
  performed.
- The public protocol envelope remains unchanged.
