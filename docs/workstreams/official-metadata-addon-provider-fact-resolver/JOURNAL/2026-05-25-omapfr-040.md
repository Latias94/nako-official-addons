# OMAPFR-040 Journal

Date: 2026-05-25
Status: DONE

## Summary

- Replaced provider catalog `external_id_aliases` with provider-owned
  `ProviderExternalIdCapability` descriptors.
- Kept the legacy `external_id_aliases()` registry API by deriving aliases from
  capability top-level fields.
- Routed `router` and `MetadataScrapeRuntime` through external ID capabilities
  while preserving the old alias constructor for compatibility tests.
- Collapsed alias and capability query parsing onto one internal descriptor path.
- Extended resolver clustering so provided catalogs limit shared external ID
  joins to capabilities marked `emits`.

## Evidence

- `cargo nextest run -p nako-metadata-scraper external_id tmdb bangumi browser_worker resolver --no-fail-fast`
- `cargo fmt --all -- --check`
- `git diff --check`

## Notes

- No reference repository code, comments, fixtures, or naming structure were
  copied.
- Host-owned refresh, locked fields, local metadata, local artwork priority, and
  final merge policy remain out of scope.
